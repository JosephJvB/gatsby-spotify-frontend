import * as React from 'react'
import { ServiceContext } from '../../gatsby-browser'
import { ModalTypes } from '../services/modalService'

// not working!
const Modals = () => {
  // const { modalService } = React.useContext(ServiceContext)
  // if (modalService.currentModal == null) {
  //   return null
  // }
  // return (
  //   <div className="modalContainer">
  //     { modalService.currentModal == ModalTypes.AccountInfo &&
  //       <div className="modalBody accountModal">
  //         <p>logout</p>
  //       </div>
  //     }
  //   </div>
  // )
}

export default Modals